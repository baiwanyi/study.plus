import { useState, useEffect } from 'react'import { advancesApi } from '@/api'import type { AdvanceSummary } from '@shared/types'export default function WidgetAdvanceStats() {    const [summary, setSummary] = useState<AdvanceSummary | null>(null)    useEffect(() => {        advancesApi            .summary()            .then((data) => {                setSummary(data)            })            .catch(() => {                setSummary(null)            })    }, [])    return (        summary &&        summary.totalPendingRepayment !== 0 && (            <div className="card space-y-2">                <p className="text-sm text-gray-700">积分预支概览</p>                <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">                    {/* 待归还积分 */}                    <div className="bg-indigo-50 rounded-lg p-3">                        <p className="text-xs text-indigo-600 font-medium mb-1">                            待归还积分                        </p>                        <p className="text-2xl font-bold text-indigo-700">                            {summary.totalPendingRepayment}                        </p>                        <p className="text-xs text-indigo-400 mt-0.5">分</p>                    </div>                    {/* 1日到期归还积分 */}                    <div className="bg-rose-50 rounded-lg p-3">                        <p className="text-xs text-rose-600 font-medium mb-1">                            1日到期归还积分                        </p>                        <p className="text-2xl font-bold text-rose-700">                            {summary.currentInstallmentDue}                        </p>                        <p className="text-xs text-rose-400 mt-0.5">                            剩余 {summary.totalRemainingInstallments} 期                        </p>                    </div>                    {/* 剩余可预支 */}                    <div className="bg-emerald-50 rounded-lg p-3">                        <p className="text-xs text-emerald-600 font-medium mb-1">                            剩余可预支                        </p>                        <p className="text-2xl font-bold text-emerald-700">                            {summary.remainingCredit}                        </p>                        <p className="text-xs text-emerald-400 mt-0.5">                            上限 {summary.maxPendingAmount} 积分                        </p>                    </div>                </div>            </div>        )    )}