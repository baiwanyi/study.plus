import Modal from '@/components/Modal'export interface WeeklyModalSaveSelectionProps {    open: boolean    analyzing: boolean    onCancel: () => void    onSaveOnly: () => void    onSaveAndAnalyze: () => void}
export default function WeeklyModalSaveSelection({    open,    analyzing,    onCancel,    onSaveOnly,    onSaveAndAnalyze,}: WeeklyModalSaveSelectionProps) {    return (        <Modal            open={open}            onCancel={onCancel}            title="选择操作"            footer={false}            size="sm">            <div className="space-y-4">                <p className="text-sm text-gray-600">                    请选择对周报执行的操作，已有分析报告的周报将不会再执行选择。                </p>                <div className="flex gap-3">                    <button                        onClick={onSaveOnly}                        disabled={analyzing}                        className="btn btn-outline flex-1">                        {analyzing ? '保存中...' : '仅保存'}
</button>                    <button                        onClick={() => {                            onCancel()                            onSaveAndAnalyze()                        }}                        disabled={analyzing}                        className="btn btn-primary flex-1">                        {analyzing ? '处理中...' : '保存并分析'}
</button>                </div>            </div>        </Modal>    )}