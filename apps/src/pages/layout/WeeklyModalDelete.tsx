import Modal from '@/components/Modal'export interface DeleteModalProps {    confirmId: number | null    onCancel: () => void    onConfirm: (id: number) => void}
export default function WeeklyModalDelete({    confirmId,    onCancel,    onConfirm,}: DeleteModalProps) {    return (        <Modal            open={confirmId !== null}            onCancel={onCancel}            onConfirm={() => {                if (confirmId !== null) onConfirm(confirmId)            }}            title="确认删除"            danger            confirmLabel="删除">            <p>确定要删除这份周报吗？此操作不可撤销。</p>        </Modal>    )}