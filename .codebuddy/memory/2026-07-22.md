# 2026-07-22

## CloudBase 空白分支创建
- 删除旧的 `cloudbase` 分支（含项目代码的 3 个提交）
- 创建新的孤立分支 `cloudbase`，仅有 1 个空根提交（`db9cf4a`）
- 推送失败：GitHub `https://github.com/baiwanyi/study.plus.git/` 443 端口无法连接（网络/代理问题）
- 待办：网络恢复后手动执行 `git push origin cloudbase --force` 推送
- 注意：推送后可能需要在 GitHub Settings → Branches 中将默认分支改回 `main`

## 学迹Plus v3.0 重构完成（study.cloudbase 项目）
- 将 study.webian.dev（Express5+SQLite+React19）按 v3.0 方案重构为 CloudBase 云函数 + TDesign 微信小程序，写入 `d:\OneDrive\Projects\study.cloudbase`
- 完成内容：
  - 公共层 common/：db.ts(MySQL)、auth.ts(JWT)、db-query.ts(userFilter注入+assertRole)、ai/client.ts(CloudBase deepseek+每日AI配额上限200防账单刷爆)、rules/summary/advance helpers、prompts/task/weekly/studynotes AI 逻辑
  - 云函数 14 个：auth、family、tasks、points、exchanges、advances(+每月1日定时还款触发器)、weekly、studynotes、options、ai-usage、categories、share-stats、init、privacy(PIPL导出/删除)
  - 小程序页面：login、dashboard、tasks、mindmap(web-view/SimpleMindMap)、weekly、studynotes、points、exchanges、borrow、ai-usage、share(Canvas2D)、options、categories、my
  - 组件：child-selector、ai-chat-panel、evaluation-report、share-card、grade-badge
  - 安全：所有查询经 userFilter；响应不暴露堆栈；隐私导出仅返回脱敏字段；AI 每日配额
- 安全修复：studynotes get 的 userFilter 曾漏在字符串字面量内（已改为模板字符串）
- 校验：miniprogram + cloudfunctions 目录 read_lints 均无错误（npx tsc 在本机不可用，未跑类型检查）
- 注意：本机 TypeScript 通过 npx 调用会命中 VS 的 tsc 垫片而失败，需用项目本地 `node_modules/.bin/tsc` 或 CI 做类型检查

## 将 ESLint 标准应用到 study.cloudbase
- 将 study.webian.dev 的 ESLint 配置标准移植到 `d:\OneDrive\Projects\study.cloudbase`，适配其双环境（Node.js 云函数 + 微信小程序）
- 创建 `eslint.config.mjs`（flat config），包含：TypeScript 命名规范（naming-convention，含 DB 列 snake_case 和 NoSQL _id 兼容）、import-x/order 排序规则、Node.js 全局变量（云函数）和 wx 全局变量（小程序）
- 新增 devDependencies：@eslint/js、eslint、eslint-plugin-import-x、globals、typescript、typescript-eslint
- 修复 119 + 18 = 137 个 ESLint 问题：移除未使用变量（ChildItem、ExchangeStatus、gradeColors、insertAndGetId、defaultGradeValues、timeoutMs、userId）、常量命名 APP/MODEL 改为 UPPER_CASE、I-前缀接口重命名（IAppOption→AppOption 等 10 个）、import 顺序自动修复、\_height 参数重命名
- 最终结果：零错误零警告通过（cloudfunctions + miniprogram 全部 .ts 文件）
## 学迹Plus PC 小程序支持 — 侧边栏布局方案

- 实现 PC 小程序侧边栏布局（左侧 240px 导航 + 内容区），适配所有 12 个页面
- **新增文件**：
  - `utils/platform.ts` — 平台检测 + 响应式工具（isPC/isWide/initPlatform/updatePlatformFromResize）
  - `app.pc.wxss` — PC 全局样式（.pc-mode 前缀隔离、侧边栏+内容布局、居中弹窗、2/3 列网格）
  - `components/pc-sidebar/` — 左侧导航组件（品牌区 + 孩子选择器 + 导航菜单 + 底部用户）
  - `components/pc-layout/` — 布局容器组件（PC 模式下显示侧边栏+内容区，移动端直接透传）
- **修改文件**：
  - `app.json` — 新增 `pc` 窗口配置（1200×800，最小 900×600，resizable）+ `window.pc` 导航栏
  - `app.ts` — 添加 platform 初始化 + onWindowResize 监听
  - 12 个页面（dashboard、tasks、points、my、weekly、studynotes、exchanges、borrow、ai-usage、share、options、categories）— 每个 .ts 添加 isWide 数据字段并读取 app.globalData.platform，.wxml 包裹 pc-layout，.json 添加 pc-layout 组件注册
- **设计要点**：
  - 子页面 child-selector 在 PC 上隐藏（已集成到侧边栏），移动端保留
  - `.pc-mode` 前缀隔离 PC 样式，与移动端零冲突
  - PC 下自动隐藏 hero 区域（侧边栏已有用户信息）和 t-fab 浮动按钮
