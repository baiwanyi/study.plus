# 2026-08-28 工作记录

## /fix：StudynotesModalShare.tsx 审查修复

- 🔴 对比度缺陷：分享长图头部底色为 theme.soft 浅色系（bg-blue-50/60 等），主题标题却是 text-white（对比度约 1:1，导出图中核心内容不可见）；改 text-gray-900 并加注释说明约束
- 🟡 无效回收：toPng 返回 base64 data URL 而非 blob URL，URL.revokeObjectURL(dataUrl) 是空操作且注释误导（声称在回收）；删除调用，注释改为说明 data URL 交由 GC 回收
- 🟡 规范：补头部 JSDoc（'use client' 首行 + 注释紧随的项目惯例）；import 按 import-x/order 重排（internal 组字母序 @apps→@components→@shared，type 组置末）
- 明确不修：effect 的 requestIdRef+cancelled 双保险（防切换/防卸载各有职责）；getTheme custom 回退；文件名非法字符清洗（均已正确处理）
- 核查确认：studynotesRouter 挂载于 /api/study，前端 /study/quiz/wrong-all 路径匹配正确，且在 requireApiKey 保护下
- 验证：apps tsc --noEmit 通过、IDE lint 0 错误

## /fix：Study/index.tsx 审查修复

- 🟡 创建课程缺 loading 守卫（handleEditSave 有 editSaving 而 handleCreate 无）：耗时期间可重复点击确认创建重复课程；新增 creating state + finally 清除 + 传入创建实例 isLoading，与编辑实例同构
- 🟡 handleDelete 固定文案吞掉服务端错误详情，与 create/edit 透传 err.message 的模式不一致；改为对齐透传
- 明确不修：activeQuizLesson 回退设计（列表刷新间隙保活，注释一致）；onClose 无条件 refetch（幂等 GET 粗粒度刷新是有意取舍）；activeQuizLesson 不加 useMemo（小数组 O(n) 无热点）；import sibling 组顺序经核查全部合规
- 验证：apps tsc --noEmit 通过、IDE lint 0 错误

## /fix：PreviewModalEditor.tsx 审查修复

- 🔴 数据丢失：加载 effect 依赖 lesson 对象引用，而 QueryClient 无 defaultOptions（refetchOnWindowFocus 默认 true）——用户填预习内容时切窗口再切回，useLessons refetch 重建 lesson 数组 → find() 产生新引用 → effect 重跑清空表单，未保存输入蒸发；修复：提取 lessonId，effect 依赖改课程 ID + 注释说明原因
- 🟡 补头部 JSDoc；保存失败吞服务端 err.message 改为透传（与 index.tsx 修复对齐）；渲染层嵌套三元（value 三级选择器）违反规范必须级 → 提取 getFieldValue/setFieldValue 辅助函数（key 类型从 previewStudyQuestions[number]['key'] 推导）；空 catch /* empty */ 补降级说明
- 待查提示：StudynotesModalEditor/LessonModalEditor 等同款 effect 依赖 lesson 对象的组件可能有同类风险（本次仅修指定文件）
- 验证：apps tsc --noEmit 通过、IDE lint 0 错误

## /fix：QuizModalEditor.tsx 审查修复

- 🔴 自动提交失效：到点自动批改直接调 grade()，但 grade 防重条件要求 quiz.submittedAt 非空（仅允许已提交未批改），作答中超时（未手动提交）会被直接 return 拒绝且 autoSubmittedRef 已锁死，主场景完全不生效（此前实现疏漏，测试仅覆盖手动链路）；修复：自动链路改为 submit() 成功后 await grade() 的两步链路（latestRef 已更新、闭包 status 非 grading 可通过），加「测验时间到，已自动提交并批改」snackbar 与手动提交对称
- 评估后不修：倒计时每秒整树重渲染（1-3ms diff 仅作答态，memo 化属无实测瓶颈的过度优化）；:576/:647 死防御（上下文已保证非空，删除违反精准修改）；倒计时 effect 依赖 remainingMs 是必要设计；radio name 冲突不成立（视图互斥+disabled）
- 验证：apps tsc --noEmit 通过、IDE lint 0 错误

## /fix：QuizModalEditor.tsx 第二轮审查修复

- 🟡 批改进行中（grading，result=null）客观题只读结果区仍渲染，!result?.isCorrect 恒 true → 所有题误标红叉「答错」几秒后自愈；EssayAnswer 已正确处理（灰字），ObjectiveAnswer 补 `result &&` 守卫
- 🟡 客观题「参考答案」直接渲染字母串 result.correctAnswer（"A,C"），与「答」行 formatAnswerText 还原格式同屏不一致（错题卡已正确还原，此处疏漏）；改用 formatAnswerText 还原并换纯文本 span
- 🟡 handleClose 不复位 sidePanel：A 课程选中的 selectedQuizId 残留到 B 课程弹窗 → isViewingHistory 立即为 true → 请求 B 课程 quiz#5 → 服务端归属校验 404 → historyQuiz 永久 undefined → 左侧永久 Loading（有【测验】按钮恢复路径但初态卡死）；关闭时 dispatch RESET
- 复核上轮自动提交两步链路 ✅ 正确；ScoreBadge 90+ amber 为设计选择非缺陷
- 验证：apps tsc --noEmit 通过、IDE lint 0 错误

## /fix：useStudynotesQuiz.ts 审查修复

- 🔴 时序竞态（上轮自动提交修复的隐患）：React 18 dispatch 重渲染走 MessageChannel 宏任务，await submit() 的 continuation 是微任务——先于渲染执行。自动链路 submit 成功后紧随的 grade() 在 latestRef.current（渲染期赋值）更新前执行，读到旧 quiz（submittedAt=null）被防重拒绝 → 自动批改静默失败；手动链路两次点击间隔渲染已完成故未暴露
- 修复：submit()/grade() 成功分支同步维护 latestRef.current（quiz: updated, status: 'graded'），附时序依据注释；一并消除 grade 成功后自动保存定时器读到旧 results 误发保存（服务端 409+提示）的次生瑕疵
- 关键经验：跨 dispatch 的微任务 continuation 中读取「渲染期赋值的 ref 镜像」必须同步维护，否则读到渲染前快照；测试仅覆盖手动链路未暴露此问题，时序类缺陷需推演 React 调度模型
- 明确不修：查询 hooks 无 staleTime（数据量小行为可预期）；渲染期 latestRef 赋值惯用法（改 useEffect 反引入陈旧窗口）；LOAD_SUCCESS answers null 元素（极端旧数据边界）
- 验证：apps tsc --noEmit 通过、IDE lint 0 错误

## /fix：useQuizCountdown.ts 审查修复

- 审查结论：无 🔴/逻辑/安全/性能缺陷。interval 漂移不受影响（每 tick 绝对时间计算 deadlineAt - Date.now()）；StrictMode 双挂载无泄漏；未激活分支 setRemainingMs(0) 值相同 React bail out；后台 tab 定时器节流/冻结后回前台 tick 按绝对时间立即恢复正确值，自动提交闭环
- 🟡 文档补全：JSDoc 注明返回值可能为负（已超时，调用方判超时用）与首帧 0（tick 前）语义，防未来调用方误假设恒为正；不加负值 clamp（会丢失「超时」信息，当前调用方已正确处理）
- 验证：IDE lint 0 错误

## /fix：useLessons.ts 审查修复

- 审查结论：逻辑/安全/边界/性能均无缺陷——normalizedSubject 归一化正确、queryKey 符合约定、subject 经 API 层 URLSearchParams 编码
- 🟡 2 处规范修复：补 'use client'（使用 useQuery，同目录 hooks 均已标注而本文件遗漏）；补三维度头部 JSDoc（归一化约束写入关键约束）
- 不修：无 staleTime（前几轮已评估，onClose refetch 粗粒度刷新是有意取舍）
- 验证：IDE lint 0 错误、apps tsc --noEmit 通过

## 修复：超时重开无「时间已到」提示

- 用户反馈：关闭弹窗超过 30 分钟再进入，倒计时徽章凭空消失（remainingMs 为负 → remainingText=null），用户不知已超时且即将被自动提交
- 修复：QuizModalEditor 新增 isTimeUp（open && isAnswering && deadlineAt 非 null && remainingMs<=0），超时作答态徽章显示「时间已到」红色脉冲（getRemainingText 辅助函数避免嵌套三元；isTimeUrgent 超时时也为 true）；自动提交+批改完成后随 isAnswering=false 自然消失
- 行为不变：关闭期间到点仍在重开时才补提交（无服务端定时任务）；正常 mm:ss 显示与 5 分钟标红逻辑不受影响
- 验证：IDE lint 0 错误、apps tsc --noEmit 通过

## 重构：倒计时全部功能封装进 useQuizCountdown

- 新 API：useQuizCountdown({ quiz, active, onTimeUp? }) → { remainingText, isTimeUrgent }；deadline 计算（generatedAt+30min，submittedAt/results 判空）、QUIZ_TIME_LIMIT_MS/QUIZ_URGENT_MS 常量、formatCountdown/getRemainingText、超时判定全部内聚
- onTimeUp 跃迁触发：tick 内检测「未超时→超时」，firedDeadlineRef（存已触发的 deadline 值）保证每 deadline 至多一次（StrictMode 双挂载/开关弹窗重跑不重复；换测验新 deadline 自然允许再触发）；onTimeUpRef 持最新引用防回调不稳定重置 effect
- QuizModalEditor 删除 ~40 行派生逻辑与每秒重跑的自动提交 effect，收敛为单行调用；remainingMs 初始 null（替代旧 0 值首帧）消除正常打开时「时间已到」闪一帧；isTimeUp 不导出（YAGNI）
- 业务动作（submit→grade→snackbar）留在组件 onTimeUp 回调，计时判定与业务分层清晰
- 验证：IDE lint 0 错误、apps tsc --noEmit 通过

## 重构：批改/重新测验按钮上移至 Modal 确认栏

- QuizHeader 删除【批改】【重新测验】按钮（仅保留标题+已批改结果摘要），QuizBody 相应删除 generate/onGrade props 透传
- Modal 确认栏改为按测验状态分发的动作状态机：作答=提交答案 / submittedAt 非空=批改 / results 非空=重新测验；锁定/历史回看/错题本/空态隐藏（confirmAction=null → onConfirm undefined）
- handleConfirm 按类型分发（submit 成功保留「请点击批改」snackbar；grade/generate 无提示与原按钮一致）；isLoading 仅 grading（generating 时 quiz 已清空无确认按钮）
- 顺带删除孤立 handleSubmit（ts6133），其提示逻辑并入 handleConfirm submit 分支
- 验证：IDE lint 0 错误、apps tsc --noEmit 通过

## /fix：QuizModalEditor.tsx（确认栏重构后复查）

- 🟡 上轮重构引入的 2 处嵌套三元自纠：confirmLabel 三分支 → CONFIRM_LABELS 映射对象；ObjectiveAnswer optionTone 五分支 → 提取 getOptionTone(isReadOnly, isSelected, isCorrectOpt) 纯函数（与 QuizSidePanel 同款修复模式一致）
- 🟡 :717 魔法数字 65 → 'A'.charCodeAt(0)；QuizConfirmAction 类型+解析 IIFE 从组件体提取为模块级 resolveConfirmAction 纯函数（可测试）
- 🟡 handleClose 声明早于 dispatchSidePanel（闭包前向引用，运行时安全但反直觉）→ 移到 reducer 声明之后
- 复核确认栏状态机时序自洽：GRADE_START 时 results 仍空显示「批改」转圈、generating 时 quiz 已清空按钮不渲染；error 双路径（有/无 quiz）自洽
- 验证：IDE lint 0 错误、apps tsc --noEmit 通过

## 修复：出题偶发 max_tokens 截断触发重试

- 现象：出题日志「[DeepSeek Empty Response] 重试 1/3，检测到 max_tokens 截断，扩容重试」——自愈机制按设计工作，非故障
- 根因：20 题输出实测 6000~14000 tokens，初始 max_tokens=12000 卡临界值，偶发超预算 → finish_reason=length 空响应 → 扩容 1.5 倍重试（用户多等一轮生成、API 费用翻倍）
- 修复：studynotes.ts 出题 max_tokens 12000→20000（批改链路已用 24000 验证模型上限充足）；core.ts 重试日志按场景分前缀 [DeepSeek Truncated]（截断自愈）/ [DeepSeek Empty Response]（疑似故障），避免误读
- 已核实：前端 axios 无超时，不会打断服务端重试链路（最坏 1s+2s+4s 等待 + 3 次全量生成）；指数退避 1s/2s/4s 已符合规范
- 验证：server tsc --noEmit 通过、IDE lint 0 错误

## 文档：README.md 更新与代码对齐

- 第 7 节学习中心：智能测验条目补 30 分钟限时；新增「历史测验与错题本」条目
- 项目结构树：utils/api → services 迁移对齐、补 quizFormat.ts、Studynotes/ 下补 components/ 目录、QuizModalEditor 与 hooks 注释更新、studynotes 路由注释补历史/错题本
- 开发计划新增 Phase 10（限时/历史回看/错题本/API 层重构/AI 健壮性）
- 自纠：结构树 QuizModalEditor 行缩进层级错一层已修正

## 文档：docs/index.html 落地页与 README 同步

- 4 处更新：「AI 智能测验」功能卡补限时/错题本；流程步骤 3「10 题测验」→「20 题限时测验 · 30 分钟倒计时到点自动提交」；步骤 4 副文案改「逐题解析 + 错题本巩固」；路线图 lg:grid-cols-4→5 并追加 Phase 9-10 卡（课程管理与课前导学/限时测验/历史回看与错题本）
- 验证经验：docs/index.html 为 65KB 单行压缩 HTML，search_content 检索不可靠（0 命中假阴性），须用 node 脚本 includes 验证；中文参数经 PowerShell 传 node 会 GBK 损坏，字符串一律用 \uXXXX 转义；div 开闭计数 208/208 平衡校验结构完整性
- 未改项：meta/JSON-LD（宽泛描述无需逐功能同步）、hero 区、技术栈卡

## 文档：docs/index.html 限时文案 30→45 分钟

- 3 处更新：AI 智能测验功能卡、流程步骤 3 副文案、Phase 9-10 路线图卡（首处 node 验证发现 Phase 9-10 卡残留一处，已补改；最终 45 分钟 3 处、30 分钟 0 残留、div 208/208 平衡）
- ⚠️ 待同步：代码 useQuizCountdown.ts QUIZ_TIME_LIMIT_MS 仍为 30 分钟、README 两处 30 分钟——用户仅要求改落地页，已提醒待确认是否同步

## /fix：StudynotesModalEditor.tsx 审查修复

- 查证排除 🔴 疑点：effect 依赖 showSnackbar，Snackbar.tsx 中 useCallback([]) 引用终身稳定，不会误触发表单清空
- 🟡 补头部 JSDoc；加载/保存失败吞服务端 err.message → 透传（catch (err: unknown) + instanceof 收窄，与此前 index.tsx/PreviewModalEditor 修复对齐）；JSON.parse 空 catch 补降级说明
- 评分失败 (:236) 固定引导文案保留：属合理设计（「内容已保存，但 AI 评估失败」传达了保存成功的关键事实）
- 审查确认无问题：isRetry return 走 finally、快照 trim 对称、cardId ?? currentCard?.id 二次保存链路、requestIdRef 防串扰、纯空格提交由服务端校验兜底
- 验证：apps tsc --noEmit 通过、IDE lint 0 错误

## /fix：QuizSidePanel.tsx 审查修复

- 🟡 历史列表 score=null（已提交未批改，服务端 getQuizHistory 只过滤 submittedAt 非空）被 `?? 0` 兜底显示红色「0 分」误导用户；改为 null 显示灰色「待批改」徽章 + 副行「已提交，等待批改」
- 🟡 WrongOptionList 嵌套三元（4 分支 tone 选择）违反规范必须级 → 提取 getOptionTone 纯函数（if 链）；:253 魔法数字 65 → 'A'.charCodeAt(0)（与 quizFormat.ts 上轮修复风格统一）
- 明确不修：score<80 红绿阈值与服务端评估线 80 同语义；历史列表内部滚动 vs 错题本外层滚动为用户分别确认的设计；text-gray-700/-mr-3 为用户手调样式
- 验证：apps tsc --noEmit 通过、IDE lint 0 错误
