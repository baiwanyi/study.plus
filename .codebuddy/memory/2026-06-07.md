# 2026-06-07 工作记录

## 作业任务添加分享功能

- 新建 `pages/layout/TaskModalShare.tsx`：分享弹窗组件，展示作业题目、作业内容、AI评分内容，支持下载为 PNG 图片
- 删除 `pages/layout/TaskModalAnalysis.tsx`（重命名为 TaskModalShare）
- 修改 `pages/Tasks.tsx`：添加 `shareTask` 状态管理和 `openShareModal`/`closeShareModal` 函数
- 修改 `pages/layout/TaskListTable.tsx`：Props 新增 `onShare` 回调，操作列增加"分享"按钮

## 作业 AI 评分历史记录

- 新增 `ai_score_logs` 表（schema + migrate）：快照记录每次AI评分时的作业内容和评分结果
- 新增 `AiScoreLog` 接口（types.ts）和 `tasksApi.aiScoreLogs()` API
- 修改 `routes/tasks.ts`：评分时插入日志快照 + 新增 `GET /:id/ai-score-logs` 路由
- 修改 `TaskEdit.tsx`：底部增加可折叠的「评分历史」面板，展示每次评分的等级、分数和评语
- 涉及文件：apps/db/schema.ts, apps/db/migrate.ts, apps/lib/types.ts, apps/lib/api.ts, apps/routes/tasks.ts, pages/layout/TaskEdit.tsx

## 作业对话持久化（每个作业保持独立对话历史）

- 新增 `task_conversations` 和 `task_messages` 表（schema + migrate）：每个作业一条会话记录，消息逐条存储
- 新增 `TaskConversation` 和 `TaskMessage` 类型（types.ts）
- 新增 `tasksApi.getConversation()` API 方法（api.ts）
- 修改 `routes/tasks.ts`：
  - 新增 `GET /:id/conversation` 路由 — 加载对话历史
  - 重写 `POST /:id/ai-demo` — 生成的示范作业保存到对话库
  - 重写 `POST /:id/ai-chat` — 保存用户消息+AI回复，使用完整历史作为上下文
- 修改 `TaskEdit.tsx`：
  - 打开作业时自动加载对话历史
  - 生成示范作业和发送消息后重新从 API 加载完整对话
- 新增 `apps/components/AiChatPanel.tsx`：可复用的 AI 聊天面板组件（上轮已建）
- 涉及文件：apps/db/schema.ts, apps/db/migrate.ts, apps/lib/types.ts, apps/lib/api.ts, apps/routes/tasks.ts, pages/layout/TaskEdit.tsx, apps/components/AiChatPanel.tsx
