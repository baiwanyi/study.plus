# 2026-09-03

## 修复：新课程「心得」按钮点击无反应

- 现象：新建课程完成预习后，操作列「心得」点击无任何响应（弹窗不打开）。
- 根因：学习中心页面心得入口存在 `studynoteId == null` 的提前返回拦截；新课程未写心得时该字段为 null，点击事件被静默丢弃。
- 修复：移除入口判空，心得未创建时以新建模式打开空白表单（cardId 为 null），保存时由 lessonId 建立课程与心得关联（后端创建接口本就要求 lessonId，编辑器组件已支持该模式）。
- 未改动处：测验按钮仍按心得评分 >= 80 的既有闸门禁用，不属于本次范围。
- 涉及文件：`apps/src/pages/Study/index.tsx`。
