# 2026-09-05

## AI 提示词接入系统设定【年级】
- `shared/src/constants.ts`：7 个 studynotes 系列提示词（心得评估 `defaultPromptEvaluateStudynotes`、预习分析 `promptAnalyzePreview`、预习课堂问答题生成/批改 `promptGeneratePreviewQuestions`/`promptGradePreviewAnswers`、专属测验客观/简答/批改 `promptStudynotesQuizObjective`/`promptStudynotesQuizEssay`/`promptStudynotesQuizGrade`）注入 `学生当前年级：{grade}` 占位符；`defaultSystemSettings` 新增 `grade: 1` 兜底。
- `server/src/services/ai/studynotes.ts`：新增 `getGradeLabel()`，通过 `loadSystemSettings()`（来自 `routes/advance-helper`）读 `settings.grade`，用 `taskClassLabels[grade]` 转为中文（如"三年级"），在各 prompt 构建处 `.replace('{grade}', ...)` 注入。公开函数签名未变，路由层（lessons.ts）无需改动。
- 系统设定年级字段 `grade` 为数字索引（0=未定级…6=六年级），OptionsSystem 与 defaultSystemSettings 默认均为 1（一年级）。
- 注意：`task title` 系列（`defaultPromptTaskTitleMindmap` 等）仍使用 `{taskGrade}`（任务级年级，由 `services/ai/task.ts` 处理），未接入系统设定年级。

## 其他当日改动（环境排障/前端）
- `apps/vite.config.ts`：proxy target 改为通过 `loadEnv` 读取 `.env` 的 `PORT`（不再写死），与 `.env` 端口保持一致。
- `apps/src/pages/Study/LessonsListTable.tsx`：预览分数、预习完成/心得完成勾选（`CheckCircle2`）均按 `scoreColor` 着色；课堂问答题用 `BadgeQuestionMark` 图标按生成/评分状态着色（灰=未评分、红<80、绿80-89、金≥90）。

## 运行期模块解析修复（ERR_MODULE_NOT_FOUND）
- `server/src/services/ai/studynotes.ts`：导入 `loadSystemSettings` 的相对路径原为 `../routes/advance-helper`，解析到不存在的 `server/src/services/routes/advance-helper`；修正为 `../../routes/advance-helper`，指向真实的 `server/src/routes/advance-helper.ts`（调用 `getGradeLabel()` 使用）。
- 课堂问答题面板（`PreviewModalEditor.tsx` + `components/PreviewQuizPanel.tsx`）：达标判断由父组件下沉到 `PreviewQuizPanel`，面板常驻显示；未达标（<80）时仅渲染琥珀色「达标要求」提示卡（含当前分数），不提供生成入口、不发起加载请求；达标后生成→作答→评分流程不变。阈值 `QUIZ_MIN_COMPLETENESS = 80` 仅在 `PreviewQuizPanel` 内定义。
- `apps/src/pages/Study/components/PreviewAnalysisReport.tsx`：完整度展示由弱化小徽章改为大圆环（SVG 双圈进度 + 中心分数 + 「完整度评分」文案），样式与着色口径（≥80 绿 / ≥60 黄 / <60 红）对齐 `EvaluationReport.tsx`。
- `README.md`：与代码对齐——数据模型清单补充 `study_preview_quiz` 表；`schema.ts` 注释表数「18→20」；学习中心「课前预习」环节补充课堂问答题（达标≥80 生成、课后作答 AI 批改、未达标提示）；项目结构补充 `PreviewQuizPanel.tsx` 组件。
- `README.md`（二次补充）：AI 使用记录分类（两处）补「预习课堂问答生成/批改」（对应 studynotes.ts 的 `preview-quiz-generate` / `preview-quiz-grade`）；核心功能列表补「数据备份（每日邮件外发）」入口；Phase 9 补预习课堂问答题条目；技术栈 AI 能力说明补「预习课堂问答生成批改」。
- `README.md`：Phase 计划新增 **Phase 11 - 数据安全与运维**，将「数据库每日邮件备份」单独成期（SMTP 发送 + IMAP 回检、默认全关、配置变量指向「数据库每日备份」章节）。
- `apps/src/pages/Study/PreviewModalEditor.tsx`：左侧预习三问表单底部新增琥珀色「预习与课堂要求」提示卡，列出三条要求（预习≥80 开启课堂问答题；不同颜色笔抄录重点与问答题到课本、课上填答并反馈；问答题批改≥80 才开启心得），与右侧未达标提示卡风格呼应。

## 心得解锁取消 80 分限制（仅心得，课堂问答题生成门槛保留）
- 规则变更：生成课堂问答题仍要求预习完整度 ≥80（服务端 `server/src/routes/lessons.ts`、`PreviewQuizPanel.QUIZ_MIN_COMPLETENESS` 均未动）；心得解锁由「问答评分 ≥80」改为「问答已出分即可」，不再看分数高低。
- `apps/src/pages/Study/index.tsx`：`canOpenReflection` 简化为 `!previewQuizGenerated || previewQuizScore != null`；拦截 toast 文案改为「完成课堂问答题后才能写心得」。
- `apps/src/pages/Study/LessonsListTable.tsx`：心得按钮 title 改为「完成课堂问答题后可写心得」；`canReflection` prop 注释同步。
- `apps/src/pages/Study/PreviewModalEditor.tsx`：要求卡第 3 条改为「须完成作答并经 AI 批改（不限分数），即可开启学习心得」。
- `shared/src/types.ts`：`previewQuizScore` 字段注释补「出分即解锁心得，不设分数门槛」。
- `README.md`：学习心得环节补「解锁门控」说明；`study_preview_quiz` 表注释补「出分即解锁心得」。
- `server/__test__/preview-quiz.e2e.test.ts`：注释同步（生成门槛用例保留不变）。
- 验证：`pnpm --filter server exec vitest run preview-quiz` 5/5 通过；server `tsc --noEmit` 通过；apps `tsc --noEmit` 仅有既有的 `Layout.tsx(83)` TS7031 报错（未改动该文件）。
