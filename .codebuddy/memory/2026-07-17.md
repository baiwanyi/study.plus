# 2026-07-17

## 修复 StudynotesModalEditor.tsx

对 `apps/src/pages/Studynotes/StudynotesModalEditor.tsx` 进行代码修复与优化：

1. **import/first 违规**：`mapMessages` 函数定义夹在两个 import 块之间，已将所有 import 移至文件顶部并按 builtin→external→internal→sibling→type 排序，函数定义置于 import 之后。
2. **全局 DOM 查询**：`useLayoutEffect` 中 `document.querySelectorAll('.form-textarea')` 会选中页面所有 textarea，新增 `formContainerRef` 并将查询限定在模态框左侧容器内。
3. **hasContentChanged 边界 bug**：snapshot 存储了未 trim 的 `stuckPoints` 与可能为 undefined 的 `memoryHook`，与 trim 后的实时值比较导致误判。已统一归一化（stuckPoints.trim()，memoryHook ?? null），并在三处 snapshot 构建点（加载、新建、保存后）保持一致。

无新增 lint 错误。

## 第二轮 fix（@command://fix 复核）

对 `apps/src/pages/Studynotes/StudynotesModalEditor.tsx` 再次审查并修复：

1. **移除不安全 `as` 断言**（194–196 行）：二次评分分支原 `card = currentCard as StudynotesCard`，改为 `if (!currentCard)` 显式守卫，避免 currentCard 为 null 时在 `card.id` 崩溃，符合类型安全规范。
2. **抽取 `runFollowUp(message?)` 复用**（原 handleFollowUp / handleChatSend）：两函数逻辑几乎相同，合并为带可选参数的单一函数，两个处理函数变为薄包装，减少重复、提升可维护性。

无新增 lint 错误。

