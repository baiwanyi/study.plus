{
    "name": "study-plus",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev:all": "concurrently \"pnpm --filter server dev\" \"pnpm --filter apps dev\"",
        "dev": "pnpm --filter apps dev",
        "build": "pnpm --filter apps build",
        "db:migrate": "pnpm --filter server db:migrate",
        "start": "pnpm --filter server start",
        "test": "pnpm --filter server exec vitest run"
    },
    "devDependencies": {
        "concurrently": "^10.0.0",
        "vitest": "^4.1.6"
    }
}
